stage('Checkout Code') {
    steps {
        git url: 'https://github.com/your-org/your-app-repo.git', branch: 'main'
    }
}

stage('Build & Test with Maven') {
    steps {
        sh 'mvn clean install'
    }
}
